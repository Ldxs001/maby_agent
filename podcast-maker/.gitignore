# ---- 独立 TTS 服务：环境与权重都不进版本库 ----
# 环境由 tts_service/setup_env.py 现建（python -m venv），动辄几个 GB；
# 权重从网上下。两者都不入库，都是「用时再下」。
tts_service/models/
tts_service/_check_out/
tts_service/.venv/

# ---- Python ----
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# ---- 运行产物与临时件 ----
*.log
server.pid
_smoke_server.log

# ---- 运行产物与试验目录 ----
# 这里只放「跑一次就完」的东西：界面截图与验收截图、探针脚本的运行产物。
# **探针脚本本身不在这里** —— 它们是可复用的工具，住在 tools/probes/（入库）。
# 因此本目录可以随时整个删掉，不会断掉任何引用（曾攒到 8 GB）。
_smoke/
/ui_*.png
